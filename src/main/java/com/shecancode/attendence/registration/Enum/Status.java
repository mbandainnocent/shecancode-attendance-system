package com.shecancode.attendence.registration.Enum;

public enum Status {
    // PENDING: admin created the student (email only) but the account is not yet
    // activated and the profile is not yet completed.
    PENDING, ACTIVE, INACTIVE, GRADUATED, DROPPED_OUT
}
